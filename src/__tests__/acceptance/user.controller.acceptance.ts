describe('UserController', () => {
  // let app: TestBackendApplication;
  // let client: Client;
  // before('setupApplication', async () => {
  //   ({app, client} = await setupApplication());
  // });
  // after(async () => {
  //   await app.stop();
  // });
  // // Login Test
  // it('invokes POST /users/login', async () => {
  //   const response = await client.post('/users/login').expect(200);
  //   expect(response.body).to.containEql({token: 'kjjak'})
  // })
});
