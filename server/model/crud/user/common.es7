const cfg = require('../../../config.json')
    , random = require('../../../lib/random.es7');


const generateId = random.getGeneratorStringHex(cfg.random.hexadecimal.charLength.document.id.user);


module.exports = {
    generateId: generateId,
}