{
  "name": "test-backend",
  "version": "0.0.1",
  "description": "User's API",
  "main": "index.js",
  "repository": {
    "url": "https://github.com/CarvAlani/test-backend-api.git"
  },
  "scripts": {
    "start": "node index",
    "dev": "nodemon index",
    "lint": "esw *.js server config --color",
    "lint:watch": "npm lint -- --watch",
    "test": "cross-env NODE_ENV=test mocha --timeout 10000 --ui bdd --reporter spec --colors tests --recursive --exit",
    "test:watch": "npm test -- --watch"
  },
  "author": "Carlos Avilan",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.1.2",
    "eslint": "^4.9.0",
    "eslint-config-airbnb-base": "^12.1.0",
    "eslint-plugin-import": "^2.8.0",
    "eslint-watch": "^3.1.3",
    "mocha": "^4.0.1",
    "nodemon": "^1.12.1",
    "supertest": "^3.0.0"
  },
  "dependencies": {
    "http-status": "^1.0.1"
  }
}
